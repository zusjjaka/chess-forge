name: CI

on:
  push:
  pull_request:

jobs:
  quality:
    runs-on: ubuntu-latest

    services:
      postgres:
        image: postgres:18
        env:
          POSTGRES_USER: auth_test
          POSTGRES_PASSWORD: root
          POSTGRES_DB: auth_test
        ports:
          - 5432:5432
        options: >-
          --health-cmd "pg_isready -U postgres -d auth_test"
          --health-interval 5s
          --health-timeout 5s
          --health-retries 5

    env:
      DATABASE_URL: postgresql+asyncpg://postgres:postgres@localhost:5432/auth_test

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Install uv
        uses: astral-sh/setup-uv@v6

      - name: Set up Python
        run: uv python install 3.13

      - name: Install dependencies
        working-directory: backend/auth
        run: uv sync --locked

      - name: Ruff
        working-directory: backend/auth
        run: |
          uv run ruff check
          uv run ruff format --check

      - name: Mypy
        working-directory: backend/auth
        run: uv run mypy .

      - name: Generate JWT keys
        working-directory: backend/auth
        run: uv run python ../../scripts/generate_keys.py

      - name: Pytest
        working-directory: backend/auth
        run: uv run pytest
